Control a non standard service
==============================

* take the my_daemon.py file, put it as root somewhere, for instance
	/opt/my_daemon.py

* control the daemon with puppet.
	* start it
	* stop it
	* restart it
